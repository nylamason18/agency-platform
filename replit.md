# Agency Platform

## Overview
A Next.js-based agency platform application.

## Project Architecture
- **Framework**: Next.js 16 with App Router
- **Language**: TypeScript
- **Styling**: CSS

## Directory Structure
```
src/
  app/
    layout.tsx    - Root layout component
    page.tsx      - Home page
    globals.css   - Global styles
```

## Development
- Run `npm run dev` to start the development server on port 5000
- The app binds to `0.0.0.0:5000` to work with Replit's proxy

## Production
- Run `npm run build` to build the application
- Run `npm run start` to start the production server

## Recent Changes
- 2025-12-21: Initial project setup with Next.js App Router
